# 更新紀錄

## V1.4.6 — Gemini 推薦單字與例句可靠性

- 可選模型更新為 Gemini 3.8／3.7／3.6／3.5 Flash、3.5／3.1 Flash-Lite 與 2.5 相容備援；預設改為 Gemini 3.8 Flash。
- 每日推薦單字改用 JSON Schema，輸出額度由 360 提高至 1400，避免思考型模型用完額度後沒有可解析的最終內容。
- 例句輸出額度提高至 1600，最多依序嘗試四個穩定模型；選定模型無權限、停用或暫時不可用時會自動切換。
- 結構化輸出被舊端點拒絕時，自動以純 JSON 指令重試；解析器同時接受陣列及 words／vocabulary／items 包裝格式。
- 429、408 與 5xx 加入有限次指數退避；不會將模型、配額、權限或格式錯誤一律誤報成網路中斷。
- 設定頁新增「測試 Gemini 連線與生成」，實際驗證 API Key、模型與 generateContent，而非只檢查有沒有儲存 Key。
- 首頁會顯示可判斷的錯誤類型，且異常回覆不會寫入每日例句紀錄。
- 新增模型切換、Schema 相容重試與回覆包裝格式回歸測試；保留手寫同頁評分及其他 V1.4.5 功能。
- 本次為前端更新，不需執行 `db:init`、部署 Worker、重建 D1 或更換 VAPID。

## V1.4.4 — 整組完成結果留在同一頁

- 最後一題評分後保留最後寫下的字與該題完整分數，不再切換至獨立結果頁。
- 同一成績區直接加入本次平均分數與達 80 分題數，不需再按「查看練習結果」。
- 最後的操作按鈕改為「完成並返回練習設定」，只負責離開已顯示完成資訊的頁面。
- 學習紀錄仍逐題保存，整組完成後的 Google Drive 同步仍會在離開手寫畫布後執行。
- 本次為純前端更新，不需執行 `db:init`、部署 Worker、重建 D1 或更換 VAPID。

## V1.4.3 — 筆跡下方直接顯示分數

- 評分後保留剛寫完的字，手機畫布自動切換成適合查看的唯讀預覽尺寸。
- 完整總分、五項分數與建議直接排列在筆跡下方，不再需要按「查看完整筆跡」。
- 手機五項分數維持三欄換行，下一題按鈕與底部導覽仍有獨立空間。
- 換到下一題時恢復原本的大畫布，不影響既有筆跡資料、評分算法或手寫效能。
- iPad 寬版維持左右分欄，評分後不需要額外操作即可看到成績。
- 本次為純前端更新，不需執行 `db:init`、部署 Worker、重建 D1 或更換 VAPID。

## V1.4.2 — iPhone 手寫評分介面

- 評分後自動切換為成績優先檢視，總分、五項分數、建議及下一題按鈕不再被大型畫布推到畫面外。
- iPhone 五項分數改為三欄換行排列，維持可讀字級與至少 44px 的操作區。
- 完整筆跡預設收合，可由成績卡展開或收起；展開只重新繪製既有筆跡，不變更評分資料。
- 書寫階段仍保留原尺寸畫布，落筆期間不切換版面；iPad 寬版配置維持不變。
- 操作列保留獨立空間並位於貼底導覽列上方，不使用覆蓋內容的懸浮按鈕。
- 本次為純前端更新，不需執行 `db:init`、部署 Worker、重建 D1 或更換 VAPID。

## V1.4.1 — 每日例句可靠性

- Gemini 例句改用 JSON Schema，固定輸出日文、完整假名與繁體中文，取消任意兩行猜測解析。
- 儲存前檢查日文、假名、繁體中文、長度及目標詞；`.)`／`Idea` 等異常內容不再進入練習紀錄。
- 只讀取模型最終輸出並檢查停止原因；截斷、空白、思考片段及格式錯誤最多重試一次。
- 同一單字的重複請求共用一次生成；設定、日期或推薦詞改變後，舊回覆不會覆蓋新資料。
- 已儲存的異常 AI 例句會隔離並以同一推薦詞重新生成；原紀錄仍保留在備份資料中但不顯示或出題。
- 今日例句與歷史紀錄在同一保存動作完成後才顯示成功；本版不需重新初始化 D1、VAPID 或部署 Worker。

## V1.4.0 — 效能與資料可靠性

- 手寫與讀音歷史改用 IndexedDB 獨立紀錄，逐題只追加一筆，不重寫完整陣列；移除舊版筆數上限。
- 手寫每批輸入只執行一次 Canvas stroke，加入輸入間隔、事件延遲、處理時間、繪製時間、長工作與評分耗時診斷。
- 啟動改為單次批量讀取 IndexedDB；版本檢查加入 8 秒逾時與一次短暫重試。
- Google Drive 同步最多三個並行下載、同一工作階段重用未變更檔案，並略過內容相同的上傳。
- 備份還原加入版本、型別、容量、筆數、checksum 驗證，所有資料在同一 IndexedDB 交易成功後才生效。
- 每日完成紀錄改為依日期排隊，保留當天最早完成時間；Worker 也以最早時間判斷是否略過通知。
- 新增 10,001 筆歷史壓力測試與 V1.4.0 資料可靠性回歸；iPad Air／iPhone 真機延遲仍需實測。

## V1.3.7 — 手寫輸入與換題效能

- 同輪練習重用 Canvas 與事件，換題只清除筆跡、更新題目與分數；發音及評分使用目前題目。
- 起筆立即顯示墨點；沿用合併輸入樣本、增量繪圖及書寫中延後縮放。
- 新增自動／僅 Apple Pencil（觸控筆）／手指（滑鼠）選項，記住選擇並包含於既有偏好備份。
- 預設關閉的書寫診斷顯示繪製時間、事件等待、中斷與縮放次數，僅在本機顯示。
- 落筆時延後外層版面變更，評分不再主動平滑捲動，操作列維持獨立貼底空間。
- 快取標準筆畫取樣及已正規化的歷史資料，保留每題本機儲存與原評分算法。
- 原有雲端同步 ready 閘門已在讀寫階段等候練習結束，沿用此機制。
- 本次是前端更新，無須重新設定 VAPID 或初始化 D1。

## V1.3.6 — 五十音讀音錯題追加補練

- 每次答錯都把該題追加到題組尾端；補練再錯仍追加，答對才不追加。
- 進度與結束判斷使用動態總題數，最後一題答錯也會繼續補練。
- 結果列出原定、補練及實際完成題數；所有答題均納入統計與既有備份。
- 維持同一輸入框及鍵盤換行送出流程。只有同一假名可補練時可能連續出現。
- 從 V1.3.5 更新只需上傳前端檔案；本次未更動資料庫結構。

## V1.3.5 — 已完成練習則略過當日通知

- 六種練習任一模式完成後，立即把完成時間送到 Cloudflare；離線時先保留，恢復連線再重試。
- Cron 在真正發送前，以訂閱時區核對當地日期及原定提醒時間；同日且在提醒時間前已完成就略過，隔天重新判斷。
- 完成狀態與 Web Push 訂閱同樣以裝置為單位；只傳送不可反推的隨機裝置雜湊，不傳 Email 或 Google Token。
- 新增兩個 D1 輔助表，既有提醒表、VAPID Keys、裝置訂閱及學習資料不重設；部署時須先執行 db:init，再部署 Worker。
- 測試通知仍會立即傳送，不受完成狀態影響，方便確認裝置通知是否正常。

## V1.3.4 — 五十音讀音隱藏分類提示

- 移除答題畫面右上角的平／片假名標籤，以及字形上方的類型、行別標籤；換題時也不再填入提示文字。
- 標題改為返回與進度兩欄，不預留第三欄；設定選項與手寫練習不變。
- 保留 V1.3.3 貼底排版、音效、鍵盤、統計、備份、通知與安全更新邏輯。完整包仍為 46 檔。

## V1.3.3 — 比照英文版貼底導覽

- 對照綠色英文版的實際入口與樣式，採預設 viewport fit，保留縮放。
- 導覽固定於視窗底部，基準高度 64px；保留 env 安全區 fallback，取消額外底部 padding，維持藍色選取狀態。
- App 預留同樣高度，避免手寫評分／下一題與內容被導覽覆蓋；>=900px 側導覽時取消底部預留。
- 保留 46 檔扁平結構、學習資料、原設定與自動／手動更新流程。程式檢查不取代 iPhone／iPad 實機驗證。

## V1.3.2 — 主畫面 PWA 底部位置與導覽間距

- 依 V1.3.1 實際截圖繼續修正，移除 App 外框 fixed／inset 定位。
- 根元素統一掌管高度：一般瀏覽器用動態視窗；standalone／fullscreen 用 100vh，body／App 沿同一文件流配置。
- 底部安全區改為 max(4px, env inset)，按鈕至少 48px，減少導覽列本身的多餘內距。
- 寬螢幕側邊導覽定位於 App 內；保留內容獨立捲動、手寫操作列、iPad 分割版面及原更新保護。
- 未改登入、學習資料、通知、同步或部署設定；46 個同層檔案。語法及程式回歸檢查不代表 iOS 實機顯示驗證。

## V1.3.1 — 手機底部留白修正

- App 外框改用視窗四邊定位，移除外框的 100dvh 高度，避免整頁高度與底部導覽採用不同基準。
- 底部導覽使用內容自動高度，移除固定高度及固定 flex-basis；底部安全區只在內距計算一次。
- 保留內容捲動區、手寫獨立操作列、iPad／桌面侧邊導覽與正常縮放。
- 更新快取與版本標記；不改動帳戶、資料、通知或部署設定。仍為 46 個同層檔案。
- 增加版面規則回歸檢查；實機 Safari／PWA 顯示仍須由裝置確認。

## V1.3.0 — 藍墨 UI 與精簡交付

- 實作已確認的蓝色設計方向：統一霧藍底、白色卡片、深藍文字與鈷藍操作。
- 首頁新增品牌列、真實本週練習圈、今日練習入口與讀音捷徑。
- 推薦詞卡顯示已保存的對應例句與中文；維持每日一詞及原例句練習紀錄。
- 練習設定保留六種模式，調整共用緊湊排版、字體與觸控尺寸。
- 手寫評分／下一題固定佔用獨立版面列，不再遮住畫布與成績；iPad 採左右欄，窄視窗自動退回單欄。
- 新 192／512px あ＋書本圖示，使用既有 KanjiVG 路徑並保留 CC BY-SA 授權。
- 修正安全區 viewport，保留頁面縮放；寬畫面導覽改為側列。
- 版本全面升為 V1_3_0；設定頁更新與安全自動更新機制保留。
- 固定交付 46 個同層檔案，整合舊部署、設定、架構、QA、更新文件；保留 10 個測試檔與 3 個授權檔。
- 不更動 OAuth、Drive Folder、Worker URL、D1、Cron 或 VAPID 設定；不重設學習偏好和本機紀錄。

## V1.2.14 — 設定順序

- 將「資料保存」與救援備份區移到設定頁最後，保留原有行為。

## V1.2.13 — 儲存與穩定性

- 改善非同步本機儲存錯誤追蹤、重試與資料保存狀態。
- 強化跨裝置學習同步與 per-device 合併，避免來源不完整時寫回。
- 新增／強化備份還原、活動狀態與更新啟用保護。
- 手寫以增量取樣、批次繪圖及中斷恢復改善卡頓，評分畫數與筆順辨識分清。

## 早期版本功能摘要

- 日文資料隔離、Google Drive、每日推播、Apple 失效訂閱修復。
- 五十音手寫與讀音、行別複選、重複練習、相鄰不重複、發音／音效與鍵盤操作。
- JLPT N1–N5、每日一詞與每日例句、連續天數及跨裝置同步。
- 六種練習模式、統計、備份／還原與手動／自動更新。

舊版完整逐次修改紀錄保留於先前 ZIP 或 Git commit；本檔自 V1.3.0 起固定追加，不再新增帶版號的文件。
