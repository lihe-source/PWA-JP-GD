# V1.2.1 更新內容

## 五十音排題修正

- 修正重複練習可能出現「え → え」或「う → う」等相同題目連續排列的問題。
- 改用平衡隨機排題：優先安排剩餘次數較多且不同於上一題的假名，再從條件相同的候選中隨機選取。
- 題目總數與每個假名的重複次數維持不變。例如あ行 5 字重複 5 次仍為 25 題，但相同假名不會相鄰。
- 當弱項篩選後只剩唯一一個假名時，因無其他題目可交錯，仍允許使用者繼續重複練習，並在設定摘要標示「單一假名重複」。

## 驗證

- 新增 100 組不同亂數種子的自動測試，每組 50 題均不得出現相鄰重複。
- 驗證每個假名的出現次數、總題數與單一假名例外情境。

## 更新方式

將 ZIP 根目錄內全部檔案上傳至 GitHub Repository 根目錄並覆蓋同名檔案。本次只更新 GitHub Pages 前端，不需重新部署 Cloudflare Worker，也不會清除 IndexedDB、Google Drive 備份或既有提醒。
