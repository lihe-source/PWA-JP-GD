# V1.2.11 更新內容

本版依 iPhone 實機錄影優化五十音手寫的落筆與連續書寫流暢度，基底為完整 V1.2.10。

## 手寫效能

- 書寫期間不再於每個 `pointermove` 清空並重畫整張畫布、格線、提示筆畫及既有筆跡。
- Apple Pencil／手指的合併取樣先存入佇列，每個畫面更新最多繪製一次，只補畫新增線段。
- 同一畫面內的多批觸控事件會共用一個 `requestAnimationFrame`，避免輸入事件量大於螢幕更新率時累積重畫工作。
- 每次落筆只量測一次畫布位置；畫布尺寸變更時才重新計算與完整重畫。
- Retina 畫布最高限制為 2 倍解析度及 1280 像素邊長，保留清楚筆跡並降低 iPhone／iPad 的像素填色負擔。
- 標準假名的 `Path2D` 在換題時預先建立，顯示提示、筆順動畫或評分後不再重複解析 SVG 路徑。
- 落筆資料、壓力值、復原、清除、提示、筆順動畫及本機評分公式維持不變。

## 測試與相容性

- 新增手寫效能測試，確認 3 倍 DPR 裝置會限制為 2 倍畫布，連續觸控事件只建立一個動畫幀，書寫中不會觸發整張畫布重畫。
- App、Manifest、Service Worker Cache、模組查詢字串與 Worker 版本統一為 `V1.2.11`／`V1_2_11`。
- 未修改 D1、備份 Schema、Google Drive、OAuth、Gemini 或推播設定，不需要重新執行 `npm run db:init`。
