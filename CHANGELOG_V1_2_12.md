# V1.2.12 更新內容

本版依 2026-09-03 的 iPhone 實機錄影，處理手寫筆跡偶發停住後一次補上的問題，基底為完整 V1.2.11。

## 找到的卡頓來源

- 影片中的筆跡最後仍完整，代表觸控資料並未遺失，而是主執行緒短暫無法更新畫面。
- 前版雖已避免整張畫布反覆重畫，但同一畫面內仍可能對每個取樣線段分別呼叫一次 Canvas `stroke()`。
- 每題評分後會排程 Google Drive 跨裝置同步；同步包含下載、合併、JSON 序列化及重新上傳，容易剛好與下一題落筆重疊。

## 手寫引擎優化

- 移除每個線段的額外佇列物件，以目前筆畫的已繪索引直接追蹤尚未顯示的取樣點，降低長時間練習的記憶體配置與垃圾回收。
- 同一動畫幀內的多個觸控點合併成一條 Canvas 路徑；一般觸控通常只需一次 `stroke()`。
- Apple Pencil 筆壓改以四分之一單位分組，只在可見寬度明顯變化時切換路徑，兼顧筆壓效果與效能。
- `pointermove`／`pointerup` 改為被動監聽；畫布繼續以 `touch-action: none` 阻止捲動，WebKit 不需等待 JavaScript 判斷取消手勢。
- 若 Safari 在書寫期間回報視窗尺寸變更，延至抬筆後才重建畫布，避免筆畫途中清除與完整重畫。

## Google Drive 同步隔離

- 每題評分後，學習天數與手寫結果仍立即寫入本機，不影響資料安全。
- 只要手寫畫布或五十音讀音輸入框仍在使用，背景 Drive 同步就延後，不與輸入搶用主執行緒。
- 完成整段手寫練習後約 0.9 秒集中同步一次，避免 45 題觸發 45 次雲端合併。

## 相容與部署

- 新增回歸檢查，驗證大量觸控取樣會批次成單一路徑、移動事件採被動監聽，以及練習期間不會啟動 Drive 同步。
- App、Manifest、Service Worker Cache、模組查詢字串與 Worker 版本統一為 `V1.2.12`／`V1_2_12`。
- 未修改 D1、備份 Schema、OAuth、Gemini、VAPID 或使用者設定，不需要重新執行 `npm run db:init`。
