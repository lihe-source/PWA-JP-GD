# V1.2.13 穩定性修正版

發布日期：2026-09-05。基礎版本：V1.2.12。

## 修正

1. 同步下載、上傳及本機寫入等待期間新增的答案，會重新讀取並合併；不以舊快照直接取代最新紀錄。
2. 同步改用每個裝置自己的 `japanese_learning_state_<deviceId>.json`，合併讀取所有裝置及舊共享檔，但只寫自己的檔案。加入資料夾篩選、分頁與部分讀取失敗保護。
3. 五十音讀音紀錄一併加入學習狀態同步，並改用現有 IndexedDB KV 儲存；既有 localStorage 紀錄啟動時會遷移。
4. IndexedDB／localStorage 寫入失敗會保留暫存、顯示警告，`flush()` 不再將失敗當作完成；可重試、匯出／匯入校驗過的完整救援 JSON。
5. 還原前等待復原點建立，等待期間資料若變更會停止自動覆寫；儲存失敗不顯示還原成功，也不寫入成功時間。
6. 手寫補齊 pointercancel、lostpointercapture、失焦與背景切換處理，不把取消事件座標當成真實筆跡。
7. Apple Pencil 可接替誤觸中的手指；防手掌誤觸期限在移動及抬筆時續期。落筆會停止筆順動畫，主動要求的重繪／尺寸調整延至抬筆。
8. 已開始的雲端請求也會在解析／合併前檢查練習狀態，而非只在請求啟動前檢查。
9. 保留日文長音「ー」；拼寫格數與輸入採一致的正規化規則。
10. 手寫評分將原本誤標的「筆順」改為「畫數」，保留舊分數相容性，不宣稱能判斷真正筆順。
11. 手寫、讀音、拼字、閱讀測驗、文章及 AI 草稿共用更新保護；儲存失敗、寫入未完成或同步／備份作業進行中，不啟用新 Worker 或重新載入。
12. App、Manifest、Service Worker、版本資訊與 Worker 顯示版本統一為 V1.2.13。

## 範圍與限制

- 本版為先前確認的第一階段 V1.2.13；完整資料庫逐筆重構、大型歷史分頁及通知診斷面板仍屬 V1.3.0 設計範圍。
- 原有手寫 2,500 筆／讀音 5,000 筆歷史上限仍存在；此前已被舊版裁掉的紀錄無法由本版重建，請保留歷史備份。
- 不包含原生 iOS 元件、伺服器端帳號系統或永久免操作 Google 授權。
- 已完成離線自動化測試；不代表已在使用者 iPhone／iPad 真機驗證流暢度、鍵盤、發音或實際推播。

詳細部署及驗收方式見 `UPDATE_V1_2_13.md` 與 `QA_V1_2_13.md`。
