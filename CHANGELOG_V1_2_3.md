# V1.2.3 更新內容

## Google 帳號與啟動流程

- 程式一開啟便直接顯示主畫面，不等待 Google 元件、登入或 Drive 比對。
- Service Worker 註冊改為非阻塞執行，iOS 檢查快取與網路時不會延遲首頁。
- 已授權的帳號會以 `prompt: none` 在背景嘗試恢復，不顯示帳號選擇或確認畫面。
- 修正空白 `prompt` 被程式忽略的問題；使用備份或還原時，已知帳號會搭配 `login_hint` 重用既有授權，不再預設要求選擇帳號。
- 背景恢復失敗不會中斷操作、不會跳登入視窗，也不會阻擋主畫面；只在實際使用雲端功能且授權確實失效時要求重新授權。
- 防止啟動期間重複發出多個背景續權請求。

## 安全與限制

- Google Access Token 維持只存於工作階段，不寫入永久儲存或備份。
- 首次連結、Google 工作階段失效、使用者撤銷權限或 Google 要求重新驗證時，仍須依 Google OAuth 規範由使用者完成一次授權。

## 版本一致性

- App、Manifest、Service Worker Cache、模組查詢字串、版本檢查與 Worker 健康檢查統一升級至 `V1.2.3`／`V1_2_3`。
- 更新靜態檔後必須執行 `npm run worker:deploy`，Worker 根網址也應顯示 `V1.2.3`。
