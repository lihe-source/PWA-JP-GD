# V1.2.4 更新內容

## 五十音手寫自動發音

- 按下「開始手寫練習」進入第一題時，立即播放該假名的日文發音。
- 每次按下「下一個假名」切換題目時，立即播放新題目的發音。
- 使用裝置內建 `ja-JP` 語音，不需要額外音檔、API Key 或網路下載。
- 原本的「🔊 發音」按鈕保留，可隨時重聽。
- 離開五十音手寫頁時立即停止尚未播完的語音，避免切換頁面後繼續朗讀。
- 「更多設定」新增「進入每一題時自動播放該假名發音」，預設開啟並記住選擇。
- 手寫自動發音與單字測驗的語音開關分離，避免先前關閉單字朗讀後導致手寫頁沒有聲音。

## iPhone／iPad 相容處理

- 自動發音直接發生在「開始」或「下一個假名」的觸控事件內，保留 iOS 所需的使用者手勢授權。
- 即使日文語音清單尚未完成載入，也會使用 `ja-JP` 預設聲線播放第一題，避免第一次沒有聲音。

## 版本一致性

- App、Manifest、Service Worker Cache、模組查詢字串、版本檢查與 Worker 健康檢查統一升級至 `V1.2.4`／`V1_2_4`。
- 上傳 GitHub 靜態檔後需執行 `npm run worker:deploy`，確保 Worker 根網址也顯示 `V1.2.4`。

