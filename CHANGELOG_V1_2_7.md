# V1.2.7 更新內容

本版以 V1.2.6 為基底，保留每日一詞、每日例句、Google 登入、Drive 備份、推播通知及五十音手寫功能。

## 五十音讀音練習

- 在練習模式的「五十音手寫」下方新增「五十音讀音」。
- 題目顯示假名，使用者輸入羅馬拼音，例如「あ」輸入 `a`。
- 可選平假名、片假名或兩者混合。
- 可複選「あ／か／さ／た／な／は／ま／や／ら／わ」行，並選擇每個假名重複 1、2、3、5 或 10 次。
- 題目採不連續隨機排列，降低同一假名連續出現的情形。
- 支援常用羅馬拼音別名，例如 `し` 可輸入 `shi` 或 `si`、`ち` 可輸入 `chi` 或 `ti`。

## 統計與備份

- 統計頁新增「五十音讀音」，顯示總答題數、答對題數、整體正確率及 92 個假名的個別正確率。
- 設定頁可匯出、匯入或清除五十音讀音 CSV。
- 完整 ZIP 與 Google Drive 備份加入 `kanaReadingHistory`。
- 備份 Schema 升級為 2，並保留對 Schema 1 舊備份的相容性。

## 版本

- App、Manifest、Service Worker Cache、模組查詢字串、版本檢查與 Worker 統一為 `V1.2.7`／`V1_2_7`。
