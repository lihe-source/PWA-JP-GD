# V1.2.8 更新內容

本版以 V1.2.7 為基底，保留 Google 登入與 Drive 同步、每日一詞、五十音手寫與讀音、統計、備份及 Cloudflare 推播通知。

## 練習頁統一與壓縮

- 以「五十音手寫」頁為共同設計基準，統一六種練習模式的標題、卡片、摘要、選項與主按鈕節奏。
- 單字拼寫把練習題數、出題順序與發音延遲整合為單一卡片，並即時更新四欄設定摘要。
- 五十音讀音直接共用手寫頁的四欄摘要、假名類型、四欄行別網格與五欄重複次數。
- 文章撰寫將出題模式、題目、輸入框與送出按鈕合併在同一卡片。
- 文章閱讀使用緊湊標題與四欄規則摘要；AI 詢問同步改為緊湊輸入卡。

## iPhone 與 iPad

- iPhone 隱藏重複頁首與次要說明，保留 44px 以上的觸控高度，讓同一畫面可看到更多選項。
- iPad 保留完整說明並運用寬版多欄排列；版面依寬度自動切換，不需要使用者手動選擇裝置模式。
- 實際作答、手寫畫布、評分、統計與資料儲存邏輯均未變更。

## 部署

- GitHub Pages 靜態檔案、Manifest、模組版本與 Service Worker Cache 已統一為 `V1.2.8`／`V1_2_8`。
- D1 Schema 沒有變更，不需要執行 `npm run db:init`。
- 若要讓 Worker 根網址顯示 V1.2.8，可執行 `npm run worker:deploy`；既有 VAPID Secrets 與 D1 資料會保留。
