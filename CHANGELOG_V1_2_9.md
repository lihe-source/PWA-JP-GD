# V1.2.9 更新內容

本版以 V1.2.8 為基底，保留全部緊湊版面、Google 登入與 Drive 同步、備份、統計及 Cloudflare 推播通知。

## 五十音讀音音效

- 答對時播放與單字拼寫相同的上升提示音。
- 答錯時播放與單字拼寫相同的錯誤提示音。
- 練習結束後依正確率播放分級結果音效。
- 在「開始讀音練習」的直接點擊事件中預先解鎖 Web Audio，降低 iOS PWA 阻擋第一個提示音的機率。

## iPhone／iPad 自動鍵盤

- 每題建立輸入框後立即同步聚焦，不再把第一次 `focus()` 延後到下一個畫面影格。
- 第一題及每次按下「下一題」後，iPhone／iPad 會自動彈出軟體鍵盤。
- 輸入框加入 `autofocus`、英文輸入語系、停用自動修正與自動大寫，避免羅馬拼音被系統改寫。
- 保留一次畫面影格補強，只在瀏覽器因版面切換失去焦點時重新聚焦。
- iPhone 鍵盤開啟時自動縮小題目假名與上方間距，讓拼音輸入框及確認按鈕維持在可視區域。

## 部署

- App、Manifest、Service Worker Cache、模組查詢字串與 Worker 版本統一為 `V1.2.9`／`V1_2_9`。
- D1 Schema、備份 Schema 與既有設定格式均未變更，不需要執行 `npm run db:init`。
