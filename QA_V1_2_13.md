# V1.2.13 驗證紀錄

## 已執行

- Node.js 自動化回歸測試：77 項通過；執行環境預設時區與 Asia/Taipei 時區皆通過。
- 30 個 JavaScript／測試檔案通過語法檢查。
- 25 個 Service Worker 預存資源及所有前端模組依賴已核對；模組版本與離線快取項目一致。
- 部署設定、推播設定、D1 Schema 與既有圖示和 V1.2.12 逐位元組比對一致；Worker 僅更新版本字串，套件鎖定版本未變更。
- 新增真實 Drive 方法隔離測試：資料夾篩選及分頁、裝置分檔、讀取失敗保護、復原點失敗、等待中新增本機資料、儲存失敗及成功完成時序。
- 新增同步測試：下載／上傳／flush 期間新增答案、有界重試、兩台裝置併發及新舊學習資料合併。
- 新增儲存測試：容量不足、刪除失敗、IndexedDB 失敗、舊修訂延遲失敗、讀音 IndexedDB 寫入。
- 新增手寫事件測試：捕捉遺失、取消、背景切換、Pencil 接替誤觸、動畫及 Resize 保護、100 筆連續書寫事件與清理。
- 新增更新與日文測試：各模式保護、儲存失敗禁止啟用／重載、寫入等待中開始練習，以及長音／小假名／濁音區別。
- 原有重複出題不連題、備份校驗、部署預設值及扁平結構測試一併執行。

在具備 Node.js 的終端機，執行 `node --test test-*.mjs` 可重跑測試。不需要 Google 或 Cloudflare 帳號。

## 驗證界線

測試使用模擬儲存、網路與 Canvas 事件，不會向真實 Google Drive 寫入，也未部署到使用者的 GitHub 或 Cloudflare。
環境沒有可用的圖形瀏覽器，瀏覽器測試元件下載逾時，因此未完成瀏覽器截圖、真實 iOS 鍵盤或裝置效能測試。
「100 筆書寫事件通過」是程式回歸結果，不是已完成 100 題 Apple Pencil 真機測試，也不保證任何裝置達到固定 FPS。

## 真機驗收清單

| 項目 | 操作 | 合格條件 |
|---|---|---|
| iPhone 15 Pro Max | 直向／橫向各寫至少 25 題 | 評分、下一題與導覽可點，筆跡不需離開頁面才能恢復 |
| iPad Air 11 吋 | Apple Pencil 書寫並讓手掌正常接觸螢幕，連續練習 100 題 | 不出現手掌新增的大筆畫；抬筆後下一筆仍可書寫 |
| 中斷恢復 | 寫到一半切背景再返回，或旋轉螢幕 | 已完成筆跡保留，下一筆可正常寫，不產生連到左上角的假線 |
| 讀音練習 | 用鍵盤換行鍵連續答題 | 正／誤回饋後換題，同一輸入框維持，不必上下尋找按鈕 |
| 跨裝置同步 | 兩台皆更新後各做不同題目，再依序同步兩次 | 雙方答案聯集合併，不減少本次新增紀錄 |
| 備份 | 上傳、合併還原、覆蓋還原、匯出／匯入救援 JSON | 有保存狀態；失敗不顯示成功；可從備份找回紀錄 |
| 安全更新 | 練習中有新版本，完成後回主頁 | 練習中不重載，保存成功後才更新 |
| 通知回歸 | 各裝置發送測試通知，另設排程 | 確認原有通知功能正常；本版未修改後端傳送邏輯 |
