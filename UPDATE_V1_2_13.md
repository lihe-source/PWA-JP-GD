# V1.2.13 更新步驟（既有使用者）

這是從 V1.2.12 升級的穩定性修正版，維持藍色日文介面與同層部署。

## 手機更新 GitHub Pages

1. 更新前，在舊版「設定」匯出學習資料備份；保留原 ZIP。
2. 解壓 `PWA-Japanese-GD-V1_2_13-FLAT.zip`，打開同名資料夾。
3. 在 GitHub 開啟 `lihe-source/PWA-JP-GD` 的 `main` 分支。
4. 使用 **Add file → Upload files**，選取資料夾內全部檔案，一次提交。
5. `index.html` 應直接位於 Repository 根目錄，不要將整個外層資料夾上傳。
6. 等候 GitHub Pages 的部署完成，再由主畫面開啟 PWA。
7. 到「設定 → 版本資訊」，確認當前版本為 **V1.2.13**；若仍為舊版，按「檢查更新」。

請同時更新 iPhone、iPad 及電腦。雲端同步格式新增裝置分檔；舊版可繼續讀取原共享檔，但不會讀取新版裝置檔。所有裝置更新後再比較統計。

## 自動更新規則

- 開啟程式會檢查 `version.json`；設定頁可手動檢查。
- 練習、輸入文章、雲端同步、備份上傳／還原或本機寫入未完成時，不切換版本、不重新載入。
- 資料成功儲存且操作結束後，才套用已下載的更新。
- 從 V1.2.12 第一次升級時，請先結束所有裝置／分頁中的練習；舊版仍使用舊版更新保護邏輯。
- 不要刪除網站資料或移除 PWA 來強制更新，以免未備份的本機紀錄遺失。

## Google 與 Cloudflare

- OAuth Client ID、Drive 資料夾 ID、Worker URL 及 D1 綁定沿用已提供的設定。
- Gemini Key 留在原裝置內，不打包至 ZIP，也不寫入公開 GitHub。
- 這次通知邏輯及 D1 Schema 沒有變更，現有通知後端可繼續使用。
- 若需要讓 Worker 根網址的版本資訊也顯示 V1.2.13，在 Codespaces 上傳新檔後執行 `npm ci`，再執行 `npm run worker:deploy`。不要執行 `db:init`，不要重新產生 VAPID Keys。
- 保留 Cloudflare 已設定的 `VAPID_PUBLIC_KEY`、`VAPID_PRIVATE_KEY`、`VAPID_SUBJECT`；不要將私密值填入前端檔案。
- Google 授權有效時會沿用；授權失效仍可能需要使用者重新授權，但不應阻擋進入主頁練習。

## 若出現「資料尚未儲存」

1. 暫勿關閉程式，按「重試儲存」。
2. 若仍失敗，立即按「匯出救援備份」，將 JSON 存至「檔案」App。
3. 確認備份已保存後，處理裝置容量或瀏覽器儲存限制。
4. 回到「設定 → 資料保存 → 匯入救援備份」，選取 JSON。匯入會驗證格式與校驗碼，採合併方式，不直接清空本機資料。
5. 狀態恢復為「本機已儲存」後，再執行雲端同步。

「本機已儲存・雲端待同步」不是資料遺失，表示本機寫入完成，但雲端尚未確認完成。

## 升級後檢查

- 先做幾題手寫與讀音練習，回設定確認保存狀態。
- iPhone 與 iPad 都更新後，各自完成練習並同步，再確認兩邊記錄合併。
- 手寫評分中的「畫數」是筆畫數符合度，不是自動認證筆順正確；筆順請對照動畫。
- 「ビール」與「ビル」會被判定為不同答案，半形「ﾋﾞｰﾙ」仍可正規化為「ビール」。
